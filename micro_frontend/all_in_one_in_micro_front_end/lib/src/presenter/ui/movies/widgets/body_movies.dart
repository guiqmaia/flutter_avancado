import 'package:flutter/material.dart';

import 'visibility_grid_view_builder.dart';

class BodyMovies extends StatelessWidget {
  const BodyMovies({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const VisibilityGridViewBuilder();
  }
}
